---

- hosts: vagrant
  become: yes
  vars:
    docker_package: "docker-ce=17.03*"
  roles:
    - geerlingguy.docker
  tags:
    - docker

- hosts: vagrant
  become: yes
  pre_tasks:
#    - file:
#        path: "{{ ka_master_config_path }}"
#        state: touch
#        mode: "u=rw,g=r,o=r"
  roles:
    - ../../../ansible-kontena-agent
  tags:
    - kontena_agent

